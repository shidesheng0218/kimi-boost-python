---
description: 按 PEP8+类型标注规范修复指定文件
---

按本预设的 Python 工程化规范修复以下文件的 lint 与类型问题:$ARGUMENTS

若上方为空,则处理当前未提交改动涉及的 .py 文件。

要求:

1. 先识别项目已有的工具链(pyproject.toml 里的 ruff/black/mypy 配置),优先使用项目已配置的工具
2. 运行项目对应的 linter 获取问题清单,再逐项修复;修复后不改变代码行为
3. 补齐缺失的类型标注,包括函数参数与返回值
4. 完成后重新运行 linter 与项目测试,确认零告警、测试通过
